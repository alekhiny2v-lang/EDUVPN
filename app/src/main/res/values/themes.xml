<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Material 3 gives us the textAppearance* attributes the layout relies on. -->
    <style name="Theme.EDUVPN" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/accent</item>
        <item name="android:colorBackground">@color/screen_background</item>
        <item name="android:statusBarColor" tools:targetApi="21">@color/screen_background</item>
        <item name="android:navigationBarColor" tools:targetApi="21">@color/screen_background</item>
    </style>

</resources>
